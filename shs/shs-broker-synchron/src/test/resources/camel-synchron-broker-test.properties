shs.ldap.url=ldap://axel-dev:389/L=SHS
# min size per LDAP pool connection entity (request one, get X)
shs.ldap.connect.pool.initsize=1
# max size per LDAP pool connection entity (pool limit per entity)
shs.ldap.connect.pool.maxsize=15
# preferred LDAP pool concurrent number of connections per connection identity
shs.ldap.connect.pool.prefsize=2
# LDAP pool idle connection drop timeout (before purge/discard from pool)
shs.ldap.connect.pool.timeout=300000

# the organization number that owns the shs-node
shs.node.owner=5565317129